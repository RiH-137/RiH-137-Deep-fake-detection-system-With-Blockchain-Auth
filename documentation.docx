1. Haar Cascade uses a cascade function trained on positive and negative images to detect objects.
 It extracts Haar features from rectangular areas in an image, 
and then combines multiple Haar features in a hierarchy to build a classifier.

2. haar are digital image features used for object recognition.

3. Improves data quality: Normalization removes duplicates, redundancies,
     inconsistencies, and anomalies, and resolves data conflicts.

4. PIL stands for Python Imaging Library, which is a library that allows
     Python to manipulate images.     
5. No. of frames are depending upon the the video length.

6.